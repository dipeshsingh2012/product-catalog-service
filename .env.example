PORT=8001
HOST=0.0.0.0
# Neon PostgreSQL database URL (or pass as SQL_DB):
DATABASE_URL=postgresql+asyncpg://<user>:<password>@<neon-host>/mycommerce-pim?ssl=require
# Or using SQL_DB secret directly:
# SQL_DB=postgresql://<user>:<password>@<neon-host>/mycommerce-pim?sslmode=require
CORS_ORIGINS=*
