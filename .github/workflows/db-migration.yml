name: Database Migration & Seed

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      action:
        description: 'Migration action to execute'
        required: true
        default: 'upgrade'
        type: choice
        options:
          - 'upgrade'
          - 'current'
          - 'history'

jobs:
  migrate:
    name: Run Alembic Migrations & Seed
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Verify Database Secret
        env:
          SQL_DB: ${{ secrets.SQL_DB }}
        run: |
          if [ -z "$SQL_DB" ]; then
            echo "::error::SQL_DB secret is not configured in GitHub repository secrets. Please add SQL_DB under Settings > Secrets and variables > Actions."
            exit 1
          fi
          echo "SQL_DB secret is present."

      - name: Apply Migrations
        env:
          SQL_DB: ${{ secrets.SQL_DB }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            case "${{ inputs.action }}" in
              "current")
                alembic current
                ;;
              "history")
                alembic history --verbose
                ;;
              *)
                alembic upgrade head
                ;;
            esac
          else
            alembic upgrade head
          fi

      - name: Seed / Sync Catalog Data (Hiljhil Roasters)
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && (inputs.action == 'upgrade' || inputs.action == '')) }}
        env:
          SQL_DB: ${{ secrets.SQL_DB }}
        run: |
          python -m src.db.seed

